name: Package Ownership
description: Warns PR authors and mentions team members when PRs cross team ownership boundaries

inputs:
  token:
    description: GitHub token
    required: false
    default: ${{ github.token }}
  ownership-file:
    description: Path to the ownership file relative to repo root
    required: false
    default: '.ownership'
  fail-on-violation:
    description: Fail the check when ownership is violated and no team member has approved
    required: false
    default: 'true'

runs:
  using: node20
  main: dist/index.js

branding:
  icon: shield
  color: orange
